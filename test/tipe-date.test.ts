describe("Data type", () => {
  it("should must declare", () => {
    let name: string = "Rizal Pradana";
    let balence: number = 2000;
    let isVip: boolean = true;

    console.info(name);
    console.info(balence);
    console.info(isVip);
  });
});
